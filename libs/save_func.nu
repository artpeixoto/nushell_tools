# export def main [func: any] {
    # let source =  view source $func
    # let source_ast = ast --flatten $source ;
    # let signature = $source_ast | where type == shape_signature | first | get content ;
    # let block = source_  
    # let name = metadata $func | view span $in.span.start $in.span.end ;  
    # let block = 
# }